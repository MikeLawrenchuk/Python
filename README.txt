# Twin Prime Number Analysis

This Python program performs two main tasks related to prime numbers:

1. It calculates the sum of prime gaps and the number of twin primes in a given range.
2. It filters out the prime numbers from a list of numbers.

## Dependencies

This program uses the sympy library in Python. You can install it using pip:

